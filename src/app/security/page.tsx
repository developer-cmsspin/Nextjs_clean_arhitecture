'use client';

import 'reflect-metadata';

export default function Test() {
  /*
    dependencyInjection();
    const source: string = "page:/test";
    const action = getDependency<ITestAction>("ITestAction");
    const loading = action.GetTestLoading();
    const client: ResponseExchange = action.GetTestData();
    action.LoadExecute();

    //Use the method useBaseTheme for improve performance
    
    return (
        <ThemeComponentClient source={source}>
            {useBaseTheme(source) ? (
                <>
                    {loading === true ? (
                        <div>loading</div>
                    ) : client ? (
                        <>
                            <div>===============================</div>
                            <div>total items {client.list.length}</div>
                            <div>===============================</div>
                            <div>
                                {client.list.map((exchange, i) => {
                                    console.log("Enter");
                                    // Return the element. Also pass key     
                                    return (<div key={exchange.id}>--{exchange.name}({exchange.value} USD)</div>)
                                })}
                            </div>
                        </>


                    ) : (
                        null
                    )
                    }
                </>) : null

            }
        </ThemeComponentClient>
    )
    */
  return <>HOLA</>;
}
